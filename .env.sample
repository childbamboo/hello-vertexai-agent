# ==============================================================================
# Environment Variables for hello-vertexai-agent
#
# Usage:
#   cp .env.sample .env
#   Edit .env and fill in your values
# ==============================================================================

# ------------------------------------------------------------------------------
# Google Cloud Platform
#
# Prerequisites:
#   1. Install gcloud CLI
#      https://cloud.google.com/sdk/docs/install
#
#   2. Login and set up application default credentials
#      gcloud auth login
#      gcloud auth application-default login
#
#   3. Create or select a GCP project
#      gcloud projects create YOUR_PROJECT_ID  # new project
#      gcloud config set project YOUR_PROJECT_ID
#
#   4. Enable billing on the project
#      https://console.cloud.google.com/billing
#
#   5. Run setup.sh to enable required APIs and create resources
#      export PROJECT_ID=your-project-id
#      bash setup.sh
#
#   setup.sh will enable the following APIs automatically:
#     - Vertex AI (aiplatform.googleapis.com)
#     - Cloud Storage (storage.googleapis.com)
#     - Logging, Monitoring, Cloud Trace
#     - Cloud API Registry (for Agent Garden, Preview)
# ------------------------------------------------------------------------------
PROJECT_ID=your-gcp-project-id
REGION=us-central1
STAGING_BUCKET=gs://your-project-id-vertexai-staging

# RAG Engine region (us-central1 is restricted for new projects)
# Supported: europe-west4, asia-southeast1, us-west1, etc.
# See: https://cloud.google.com/vertex-ai/generative-ai/docs/rag-engine/rag-overview#supported-regions
RAG_REGION=europe-west4

# ------------------------------------------------------------------------------
# Finnhub (Required for 05_multi_agent)
# Free tier: 60 requests/min
# Sign up: https://finnhub.io/register
# API key: https://finnhub.io/dashboard (after login)
# ------------------------------------------------------------------------------
FINNHUB_API_KEY=

# ------------------------------------------------------------------------------
# FRED - Federal Reserve Economic Data (Optional)
# Free, 120 requests/min
# Sign up: https://fredaccount.stlouisfed.org/login/secure/
# API key: https://fredaccount.stlouisfed.org/apikeys (after login)
# ------------------------------------------------------------------------------
FRED_API_KEY=

# ------------------------------------------------------------------------------
# Reddit (Optional)
# Free, 100 requests/min (OAuth)
# Setup:
#   1. Go to https://www.reddit.com/prefs/apps
#   2. Click "create another app..."
#   3. Select "script" type
#   4. Set redirect URI to http://localhost:8080
#   5. Copy the client ID (under app name) and secret
# ------------------------------------------------------------------------------
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=

# ------------------------------------------------------------------------------
# Marketaux (Optional)
# Free tier: 100 requests/day
# Sign up: https://www.marketaux.com/register
# API key: https://www.marketaux.com/account/dashboard (after login)
# ------------------------------------------------------------------------------
MARKETAUX_API_KEY=

# ------------------------------------------------------------------------------
# Financial Datasets - MCP integration (Optional)
# Sign up: https://financialdatasets.ai/
# ------------------------------------------------------------------------------
FINANCIAL_DATASETS_API_KEY=
